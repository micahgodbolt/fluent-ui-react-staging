{
  "name": "fluent-ui-monorepo",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "scripts"
    ]
  },
  "scripts": {
    "build": "lerna run build --stream",
    "start": "yarn workspace @fluentui/react start"
  },
  "devDependencies": {
    "lerna": "^3.18.1",
    "beachball": "^1.14.1"
  }
}
