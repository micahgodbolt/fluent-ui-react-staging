{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "scripts"
    ]
  },
  "scripts": {
    "build": "lerna run build --stream",
    "start": "yarn storybook",
    "storybook": "lerna --scope @fluentui/scripts run build && yarn workspace @fluentui/react start"
  },
  "devDependencies": {
    "lerna": "^3.18.1",
    "beachball": "^1.14.1"
  }
}
