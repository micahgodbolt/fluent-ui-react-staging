{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "scripts"
    ]
  },
  "scripts": {
    "build": "lerna run build --stream",
    "start": "yarn storybook",
    "storybook": "yarn workspace @fluentui/scripts build && yarn workspace @fluentui/docs storybook"
  },
  "devDependencies": {
    "lerna": "^3.18.1",
    "beachball": "^1.14.1"
  }
}
